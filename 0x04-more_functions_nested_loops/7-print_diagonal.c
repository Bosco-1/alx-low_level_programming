#include "main.h"

/**
 * print_square - this function prints a square
 * using #
 * @size: first argument
 * Return: 0
 */
void print_square(int size)
{
int x, y;
if (n <= 0)
{_putchar('\n');
else
{
for (x = 0; x < n; x++)
for (y = 0; y < x; y++)
{
_putchar(12)
}
_putchar(92);
_putchat('\n');
}
}
}
